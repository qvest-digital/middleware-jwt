language: go
sudo: false

matrix:
  include:
    - go: 1.11
    - go: 1.12
    - go: 1.13
    - go: tip
  allow_failures:
    - go: tip

install:
  - # skip

script:
  - go get -t -v ./...
  - diff -u <(echo -n) <(gofmt -d .)
  - go vet $(go list ./...)
  - go test -v -race ./...